<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Core schema + reference data (consolidated from the etalon DB). -->
    <include file="db/changelog/001-core-schema.xml"/>
    <include file="db/changelog/002-core-reference-data.xml"/>

    <!-- Bestiary (kept verbatim): schema, dictionaries, monsters, authoring, enum dicts. -->
    <include file="db/changelog/040-create-bestiary-schema.xml"/>
    <include file="db/changelog/041-seed-bestiary-dictionaries.xml"/>
    <include file="db/changelog/042-seed-bestiary-monsters.xml"/>
    <include file="db/changelog/043-bestiary-monster-authoring.xml"/>
    <include file="db/changelog/044-bestiary-enum-dicts-and-homebrew.xml"/>

    <!-- Core <-> bestiary FK links (deferred from 001). -->
    <include file="db/changelog/045-link-core-bestiary-fks.xml"/>

    <!-- NPC roles (merchant, quest giver, …) and merchant trading. -->
    <include file="db/changelog/050-npc-roles-and-trading.xml"/>

    <!-- Per-combatant action economy (action / bonus action / reaction). -->
    <include file="db/changelog/051-combat-action-economy.xml"/>

    <!-- Active effect -> source item instance, so unequip removes only that item's buffs. -->
    <include file="db/changelog/052-active-effect-source-item.xml"/>

    <!-- Server-side refresh-token sessions: rotation + reuse detection. -->
    <include file="db/changelog/053-refresh-token-sessions.xml"/>

    <!-- Item instance -> buff/debuff links for instance-specific item effects. -->
    <include file="db/changelog/054-item-instance-buffs.xml"/>

    <!-- User-submitted frontend diagnostics and optional bug descriptions. -->
    <include file="db/changelog/055-bug-reports.xml"/>

    <!-- Structured spell damage (base dice + damage type), seeded from detected_damage[]. -->
    <include file="db/changelog/056-spell-damage.xml"/>

    <!-- Structured spell save ability + attack-roll flag, parsed from the RU description. -->
    <include file="db/changelog/057-spell-save.xml"/>

    <!-- Data-quality flag for spells whose auto-derived resolution needs manual review. -->
    <include file="db/changelog/058-spell-warning.xml"/>

    <!-- Summon-spell -> bestiary statblock links (Find Familiar, Phantom Steed, Summon X). -->
    <include file="db/changelog/059-spell-summon-monster.xml"/>

    <!-- Structured spell healing (dice / flat HP restored), parsed from the RU description. -->
    <include file="db/changelog/060-spell-healing.xml"/>

    <!-- Ability-check resolution (проверка Интеллекта (Расследование)): ability + skill. -->
    <include file="db/changelog/061-spell-check.xml"/>

    <!-- Spell -> buff/debuff links: a spell can apply one or more effects. -->
    <include file="db/changelog/062-spell-buffs.xml"/>

    <!-- Countable action economy: action/bonus/legendary pools replace the boolean flags. -->
    <include file="db/changelog/063-combat-action-counts.xml"/>

    <!-- Structured class-feature mechanics parsed from descriptions + admin warning flags. -->
    <include file="db/changelog/064-class-feature-mechanics.xml"/>

    <!-- Social graph: friend requests, friendships and blocks (user_relationships). -->
    <include file="db/changelog/065-friends.xml"/>

    <!-- Feature rules runtime — Stage 1 foundation: feature_rule, feature_rule_issue + reference vocabularies. -->
    <include file="db/changelog/066-feature-rule-foundation.xml"/>

    <!-- Feature rules runtime — Stage 2: rule revisions + game source/ruleset scope. -->
    <include file="db/changelog/067-feature-rule-versioning.xml"/>

    <!-- Feature rules runtime — Stage 3: bounded formula/predicate engine (feature_formula). -->
    <include file="db/changelog/068-feature-formula.xml"/>

    <!-- Feature rules runtime — Stage 4: static grants + level-up choices (parallel, flag-gated). -->
    <include file="db/changelog/069-feature-grants-and-choices.xml"/>

    <!-- Feature rules runtime — Stage 5: feature resources, charges and rest recovery. -->
    <include file="db/changelog/070-feature-resources.xml"/>

    <!-- Feature rules runtime — Stage 6: feature action costs + feature-use audit log. -->
    <include file="db/changelog/071-feature-action-costs.xml"/>

    <!-- Feature rules runtime — Stage 7: active effects, modifiers, end conditions (separate, flag-gated). -->
    <include file="db/changelog/072-feature-effects.xml"/>

    <!-- Feature rules runtime — Stage 8: structured combat resolution (damage/healing/save/attack rules). -->
    <include file="db/changelog/073-feature-combat-rules.xml"/>

    <!-- Feature rules runtime — Stage 9: feature spell grants/filters (additive over the spell model). -->
    <include file="db/changelog/074-feature-spell-grants.xml"/>

    <!-- Feature rules runtime — Stage 10: forms/transformations/companions (Wild Shape-like, flag-gated). -->
    <include file="db/changelog/075-feature-forms-companions.xml"/>

    <!-- Feature rules runtime — Stage 11: triggers, reactions, durable gameplay prompts. -->
    <include file="db/changelog/076-feature-triggers.xml"/>

    <!-- Class-aware UI: per-class spellcasting metadata (caster type, prepared/known, spellbook, ritual). -->
    <include file="db/changelog/077-class-spellcasting-metadata.xml"/>

    <!-- Resource unification: max formula on custom_resource_types (formula/level-scaling maxima). -->
    <include file="db/changelog/078-resource-max-formula.xml"/>

    <!-- Rest reset: reset_on window on custom_resource_types (short/long rest refill). -->
    <include file="db/changelog/079-resource-reset-on.xml"/>

    <!-- Feat-bound resources: feat_bound_id on custom_resource_types (feat-granted resources, e.g. Luck Points). -->
    <include file="db/changelog/080-resource-feat-bound.xml"/>

    <!-- Rich rest recovery: independent short/long-rest recovery (none|full|formula) on custom_resource_types. -->
    <include file="db/changelog/081-resource-rest-recovery.xml"/>

    <!-- Polymorphic feature-rule owner (S1): drop owner→class_feature FKs, composite owner index, choice owner_type. -->
    <include file="db/changelog/082-feature-rule-polymorphic-owner.xml"/>

    <!-- Per-character feats (S1, FEAT owner): structured character↔feat table replacing the features text blob. -->
    <include file="db/changelog/083-character-feats.xml"/>

    <!-- Unify the 3 damage-type tables into one canonical damage_type (repoint all FKs, drop the other two). -->
    <include file="db/changelog/084-merge-damage-types.xml"/>

    <!-- Structured per-character hit dice (short-rest spend, long-rest half-restore). -->
    <include file="db/changelog/085-character-hit-dice.xml"/>

    <!-- Rule Workbench: typed editors for standalone formulas and companion definitions. -->
    <include file="db/changelog/086-rule-workbench-generic-editors.xml"/>

    <!-- Server-authoritative per-turn movement budget (movement_used_ft), reset on turn start. -->
    <include file="db/changelog/087-battle-movement-budget.xml"/>

    <!-- Live condition instances on battle combatants (Blinded/Prone/…), linked to bestiary_conditions. -->
    <include file="db/changelog/088-battle-combatant-conditions.xml"/>

    <!-- Persistent per-battle combat log (append-only, seq-ordered, GM_ONLY-gated) — Phase 1.2. -->
    <include file="db/changelog/089-battle-log.xml"/>
    <include file="db/changelog/090-battle-concentration-check.xml"/>
    <include file="db/changelog/091-spell-area-zone.xml"/>
    <include file="db/changelog/092-battle-standard-actions.xml"/>
    <include file="db/changelog/093-trusted-device-accounts.xml"/>
    <include file="db/changelog/094-battle-monster-runtime.xml"/>
    <include file="db/changelog/095-battle-hidden-identity.xml"/>
    <include file="db/changelog/096-battle-speed-override.xml"/>
    <include file="db/changelog/097-battle-flying.xml"/>
    <include file="db/changelog/098-battle-log-undo.xml"/>
    <include file="db/changelog/099-battle-surprise-ready.xml"/>

    <!-- P0-5 / DB-1: partial unique vanilla-slug indexes (homebrew_id IS NULL) for the 6 tables
         that only had *_hb_slug, protecting the system slug namespace from duplicates. -->
    <include file="db/changelog/100-homebrew-vanilla-slug-indexes.xml"/>

    <!-- P1-4 / DB-3: created_by/updated_by (→ users) on homebrew-authored content (skills, feat,
         item_types, buffs_debuffs) + backfill from the owning package author. -->
    <include file="db/changelog/101-homebrew-content-created-by.xml"/>

    <!-- P2-6: post-moderation — homebrew_reports table (user reports on published packages). -->
    <include file="db/changelog/102-homebrew-reports.xml"/>

    <!-- ITEM_ABIL_PLAN Phase 1: item-owned feature-rules, attunement and item-scoped resources. -->
    <include file="db/changelog/103-item-feature-rules.xml"/>

    <!-- ABIL/ITEM_ABIL: persistent clientCommandId dedup for battle commands. -->
    <include file="db/changelog/104-battle-command-idempotency.xml"/>

    <!-- P1-3 + P1-4 остаток: feature_rule scope hard FKs (homebrew_pack_id, campaign_id) + created_by;
         created_by/updated_by on spell, custom_resource_types, item_templates, class_feature + backfill. -->
    <include file="db/changelog/105-feature-rule-fk-and-created-by.xml"/>

    <!-- P0.5 / SP-1: species authoring — active + authoring_json + created_by/updated_by. -->
    <include file="db/changelog/106-species-authoring.xml"/>

    <!-- ABIL: replay body for idempotent battle commands such as use-ability. -->
    <include file="db/changelog/107-battle-command-replay.xml"/>

    <!-- P1-4 хвост: created_by/updated_by на magic_item, equipment_item, background (IT-2/IT-3 + P2-3). -->
    <include file="db/changelog/108-item-background-created-by.xml"/>

</databaseChangeLog>

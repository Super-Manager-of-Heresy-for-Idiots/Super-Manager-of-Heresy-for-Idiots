package com.dnd.app.domain.enums;

/**
 * Перечисление RaceTraitRecharge описывает перечисление домена, которое фиксирует допустимые значения игровой бизнес-логики.
 * Используется для сохранения явной роли элемента в бизнес-потоке приложения.
 */
public enum RaceTraitRecharge {
    NONE,
    SHORT_REST,
    LONG_REST,
    PROFICIENCY_BONUS_PER_LONG_REST,
    CUSTOM
}

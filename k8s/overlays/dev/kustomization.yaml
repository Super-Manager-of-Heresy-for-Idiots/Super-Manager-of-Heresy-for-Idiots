apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dndapp

resources:
  - ../../base

patches:
  - path: patches/backend-replicas.yaml
  - path: patches/postgres-storage.yaml

images:
  - name: dndapp-backend
    newName: dndapp-backend
    newTag: dev
